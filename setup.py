# * 这段代码是使用 Python 的 setuptools 模块来配置和安装 Python 包。
# *
# * setup() 函数是 setuptools 提供的一个函数，用于设置包的元数据，比如包的名称、版本、作者信息等。
# * 通常在这个函数中，你会传入一些参数来描述你的包，比如 name（名称）、version（版本）、author（作者）等等。
# * 这些信息将被用来在安装时展示给用户，也会被上传到 PyPI 等包管理平台。
from setuptools import setup

setup()
